# Base URL of the dir2mcp server (from "dir2mcp up" output, e.g. http://127.0.0.1:52143)
NEXT_PUBLIC_API_URL=http://127.0.0.1:52143

# Bearer token for /api/corpus proxy (copy from dir2mcp up output; server-only, never exposed to browser)
API_TOKEN=
